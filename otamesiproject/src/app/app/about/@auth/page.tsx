import { sleep } from "@/src/lib/sleep";

export default async function auth() {
  await sleep(4000);

  //  throw new Error("Not implemented");
  return <main className="text-red-500">@authコンポート</main>;
}
